import { LoginsFilterPipe } from './logins-filter.pipe';

describe('LoginsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LoginsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
